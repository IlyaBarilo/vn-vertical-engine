# 🎮 VN Engine — Спецификация сценария и мини-игр (для авторов и нейросетей)

Документ одновременно:
- 📖 удобен для человека
- 🤖 строг для нейросети

Этот файл является:
- полной спецификацией языка сценариев
- контрактом для интеграции мини-игр
- источником истины для генерации игр через нейросети

Используйте его как:
- руководство для написания сценариев
- инструкцию для разработки мини-игр
- промпт для генерации игр

В спецификации описано:
- структура сценария (`[meta]`, `[scene]`, `[game]` и др.)
- синтаксис команд (`scene`, `bg`, `if`, `game`)
- работа с переменными
- интеграция мини-игр через iframe
- строгий контракт для разработки игр (подходит для нейросетей)

Рекомендуется:
- авторам новелл — перед написанием сценария  
- разработчикам мини-игр — перед реализацией игры

---

# 📁 ОБЩАЯ СТРУКТУРА

Сценарий (`story.js`) состоит из секций:

* [meta] — метаданные сценария
* [bg] — список фоновых изображений
* [char] — описание персонажей
* [audio] — аудио ресурсы
* [game] — реестр мини-игр
* [var] — переменные сценария
* [scene] — сцены и логика

---

# 🔧 СЕКЦИИ

### [meta] — метаданные сценария

Определяет общие параметры новеллы.

```text
[meta]
title: Название
startScene: intro
```

Формат:
```text
<key>: <value>
```

где:
- `<key>` — имя параметра
- `<value>` — значение параметра


#### Поддерживаемые параметры

##### title

```text
title: Название
```

- описание: название новеллы (отображается в интерфейсе)

---

##### startScene

```text
startScene: intro
```

- описание: идентификатор стартовой сцены  
- должен соответствовать `scene <sceneId>`

---

#### Правила

- секция `[meta]` должна быть объявлена один раз
- параметры задаются в формате `<key>: <value>`
- неизвестные параметры игнорируются





---

### [bg] — список фоновых изображений

```text
[bg]
hall = assets/backgrounds/bg_hall.jpg
```

Формат:
```text
<bgId> = <imagePath>
```

где:
- `<bgId>` — идентификатор фона
- `<imagePath>` — путь к изображению

Использование:
```text
bg hall
```

---

### [char] — описание персонажей

```text
[char]
anna image neutral = assets/characters/anna.png
anna name = "Анна"
anna color = #0F0
```

Форматы:

```text
<charId> image <state> = <imagePath>
<charId> name = <string>
<charId> color = <color>
```

где:
- `<charId>` — идентификатор персонажа
- `<state>` — состояние (эмоция)
- `<imagePath>` — путь к изображению


---

### [audio] — аудио ресурсы
```text
[audio]
bgmDay = assets/audio/day.mp3
```

Формат:
```text
<audioId> = <audioPath>
```

---

### [game] — реестр мини-игр

```text
[game]
coffeeRush = assets/games/coffee_rush.html
```

Формат:
```text
<gameId> = <htmlPath>
```

где:
- `<gameId>` — идентификатор игры. Должен быть уникальным в рамках сценария
- `<htmlPath>` — путь к HTML файлу

---

### [var]

```text
[var]
score = 0
```

Формат:
```text
<varName> = <value>
```

где:
- `<varName>` — имя переменной
- `<value>` — начальное значение:
  - number
  - string
  - boolean

Особенности:
- переменные используются в логике (`if`, `set`)
- могут изменяться во время выполнения
- могут хранить результат мини-игр


---

# 🎬 СЦЕНЫ

```text
scene <sceneId>
```

Пример:
```text
scene intro

bg hall
show anna neutral

anna: "Привет!"
```

где:
- `<sceneId>` — уникальный идентификатор сцены

Сцена содержит:
- команды управления (`bg`, `show`, `game`, `goto`)
- диалоги
- переходы

---

# 🔀 ЛОГИКА

## Переход

```text
goto <sceneId>
```

где:
- `<sceneId>` — идентификатор сцены назначения

## Условие

```text
if <expression> -> <sceneId>
```

Пример:
```text
if score == 1 -> good
if score == 0 -> bad
```

где:
- `<expression>` — логическое выражение
- `<sceneId>` — сцена перехода

Поддерживается:
- `==`, `!=`, `>`, `<`, `>=`, `<=`

---

# 🎮 МИНИ-ИГРЫ


## 📌 Общая идея

Мини-игра — это отдельный HTML-файл, который:
- загружается в iframe
- изолирована от движка
- получает параметры от движка
- возвращает результат

---

## ▶️ Запуск игры в сценарии

game coffeeRush difficulty=3 result=resultCoffee

### Параметры

| Параметр | Обязательный | Описание |
|--------|------------|--------|
| gameId | ✅ | имя игры из [game], например, coffeeRush |
| difficulty | ❌ | сложность (обычно в диапазоне 1–5), по умолчанию 3 |
| result | ✅ | имя переменной для результата, например, resultCoffee |
| любые другие | ❌ | передаются в игру |

---

## 📥 ВХОД В ИГРУ

Игра получает:

```json
{
  "type": "gameInit",
  "gameId": "coffeeRush",
  "difficulty": 3
}
```

Дополнительно:
- могут передаваться любые другие параметры (например: speed, time, targetScore)
- игра должна игнорировать неизвестные параметры

---

## ❗ СТРОГИЙ КОНТРАКТ (ОБЯЗАТЕЛЬНО)

Движок и игра взаимодействуют через `postMessage`.

Формат:
- вход: `gameInit`
- выход: `gameResult`

Игра ОБЯЗАНА:

1. Слушать message type="gameInit"
2. Использовать difficulty
3. Если в игре есть управляемые или движущиеся объекты, их скорости, ускорения и максимальные пределы должны быть явно заданы в коде через отдельные переменные, коэффициенты или параметры конфигурации, а не скрыты внутри трудноизменяемых формул. Итоговые значения могут зависеть от difficulty и других входных параметров игры.
3.1. Если в игре есть управляемые или движущиеся объекты, их перемещение, ускорение, замедление и иные изменения состояния, зависящие от времени, не должны зависеть от FPS, частоты обновления экрана или производительности устройства. Такая логика должна рассчитываться с учётом времени между кадрами (delta time / dt), чтобы поведение игры ощущалось одинаково на разных устройствах.
3.2. Для управляемых и движущихся объектов изменение разрешения экрана или размеров canvas не должно менять время прохождения одинаковой относительной дистанции на игровом поле, если иное явно не предусмотрено игровой механикой.
4. При финальной фиксации результата для движка ОБЯЗАНА отправить:

parent.postMessage({
  type: "gameResult",
  result: resultValue
}, "*");

Финальная фиксация результата для движка — это момент, когда:
- игрок подтвердил итоговый результат
- игрок отказался от дальнейших попыток
- игра была досрочно прервана

Подтверждением итогового результата считается любое действие, после которого игра больше не предлагает повторную попытку и фиксирует результат для движка.

5. Отправить РОВНО 1 раз (повторная отправка запрещена)
6. НЕ перезагружать страницу
7. НЕ менять location
8. Поле result ОБЯЗАТЕЛЬНО должно быть числом
9. Если result отсутствует → считается 0
10. После отправки результата игра должна прекратить взаимодействие с пользователем. До отправки результата игра может показывать промежуточные экраны попытки, включая победу/поражение и кнопку повторной попытки, если это предусмотрено дизайном.

---

## 🔁 Повторная попытка до фиксации результата

Игра может содержать одну или несколько внутренних попыток до отправки `gameResult`.

Для интеграции с движком значимым моментом считается именно финальная фиксация результата для движка, а не локальный экран победы/поражения одной попытки.

До отправки `gameResult` игра может:
- показывать экран успеха или поражения
- предлагать кнопку `Повторить`
- запускать новую попытку без перезагрузки страницы

При этом:
- `gameResult` отправляется ровно один раз
- отправляется только после финальной фиксации результата для движка
- после отправки `gameResult` дальнейшее взаимодействие с пользователем запрещено
- если было несколько попыток, в движок передаётся только результат последней зафиксированной попытки

---

## 📊 RESULT

Если игра поддерживает повторные попытки до отправки `gameResult`,
итоговым считается результат последней зафиксированной попытки.
Промежуточные результаты отдельных попыток в движок не отправляются.

- тип: integer
- 0 → fail
- 1 → success
- допускаются другие числа
- используется в условиях сценария (`if`)

---

## 🏁 Финальный статус игры

- если result = 1, экран должен явно показывать успех
- если result = 0, экран не должен выглядеть как победа
- промежуточные оценки допустимы, но не должны подменять success/fail
- если игра использует targetScore, targetTime или иной явный порог, итоговый success/fail должен определяться именно этим порогом
- текст, цвет, иконки и общая визуальная подача финального экрана не должны противоречить значению result

---

## 📊 HUD и игровые индикаторы

- если элемент выглядит как прогрессбар, он обязан показывать реальное значение
- шкалы должны отражать счёт, прогресс до цели, время или другой настоящий ресурс
- при превышении цели шкала остаётся заполненной на 100%; игра может завершиться успешно сразу или продолжить работу без визуального переполнения шкалы
- игрок должен понимать, что показывает каждый важный элемент HUD

---

## 🎯 В СЦЕНАРИИ

game coffeeRush difficulty=3 result=resultCoffee

if resultCoffee == 1 -> good
if resultCoffee == 0 -> bad

---

## ⚙️ ПАРАМЕТРЫ

Любые параметры разрешены:

game coffeeRush difficulty=4 speed=2 result=resultCoffee

Игра должна игнорировать неизвестные поля.

---

## ❌ ЗАПРЕЩЕНО

- fetch
- API
- CDN
- alert UI
- несколько gameResult

---

## 🚫 АНТИ-ПАТТЕРНЫ ДЛЯ ИГР

Следующие подходы считаются ошибками проектирования и должны избегаться:

### ❌ Управление и UX

- размещение элементов управления только в верхней части экрана  
- размещение элементов управления у самого нижнего края экрана  
- мелкие кнопки, по которым сложно попасть пальцем  
- разнесённые по экрану кнопки управления (нет группировки)  
- отсутствие визуальной обратной связи при нажатии  

---

### ❌ Игровой процесс

- длительное ожидание без действий (более 1–2 секунд без интерактива)
- слишком длинная игра (рекомендуется ≤ 60 секунд)
- отсутствие понятной цели (игрок не понимает, что делать)
- резкое начало без объяснения (нет визуального или игрового контекста)

---

### ❌ Интеграция с движком

- игра не отправляет `gameResult`  
- отправляет `gameResult` несколько раз  
- отправляет `gameResult` с неверным форматом (не число)  
- использует асинхронную загрузку перед стартом игры  
- зависит от внешних ресурсов (API, CDN)  
- игра отправляет `gameResult`, но продолжает позволять `retry` или изменение исхода
- игра фиксирует промежуточный результат до того, как игрок завершил серию попыток

---

### ❌ Технические ошибки

- использование `alert`, `confirm`, `prompt` как основного UI  
- блокирующие операции, зависания  
- необходимость скролла внутри игры  
- фиксированный layout, не адаптированный под вертикальный экран  
- элементы выходят за пределы экрана  
- отсутствие единой системы масштабирования UI и игровых объектов
- интерфейс визуально уменьшается на больших экранах из-за отсутствия единого масштабирования
- декоративные элементы, визуально похожие на прогрессбар или игровой индикатор, но не отражающие реальное значение
- декоративная обводка, псевдо-контур или избыточная тень у текста, если это ухудшает читаемость, визуально утяжеляет интерфейс или создаёт ложное ощущение отдельного интерактивного элемента
- зависимость скорости движения, анимации или отклика управления от FPS, частоты обновления экрана или производительности устройства
- изменение времени прохождения объектом одинаковой относительной дистанции по игровому полю при смене разрешения экрана или размеров canvas, если это не является явной частью игровой механики

---

### ❌ Поведение после завершения

- игра продолжается после отправки результата  
- пользователь может продолжать нажимать кнопки после завершения  
- отсутствует визуальный сигнал окончания игры  

---

### 🧠 Итог

Если игра содержит один или несколько пунктов выше — она считается реализованной некорректно и может работать нестабильно в рамках движка.


## ⚠️ ДОСРОЧНОЕ ЗАВЕРШЕНИЕ

При досрочном завершении (выход, ошибка, прерывание) игра должна отправить:

parent.postMessage({
  type: "gameResult",
  result: 0
}, "*");

---

## 🎮 UX

- игра должна запускаться сразу после получения параметров
- после получения gameInit игра должна переходить в активное состояние без дополнительного подтверждения пользователя, кроме случаев, когда механика игры требует обязательного обучающего шага или обязательного краткого экрана с пояснением перед началом
- обязательный экран с пояснением перед началом должен быть коротким, не содержать искусственной задержки и вести к началу игры одним явным действием пользователя
- после получения параметров игра должна начинаться без искусственной задержки; допустима только минимальная техническая пауза, необходимая для инициализации интерфейса и игровых объектов
- управление должно поддерживать:
  - touch
  - mouse
- управление и движение объектов должны ощущаться сопоставимо при touch и mouse и не должны заметно меняться из-за различий в FPS или частоте обновления экрана на разных устройствах
- интерфейс должен быть оптимизирован под вертикальную ориентацию экрана

### 📐 Масштабирование интерфейса (ОБЯЗАТЕЛЬНО)

Игра должна использовать единый коэффициент масштабирования UI, рассчитанный от базового портретного макета.

Рекомендуемый базовый размер:
- width = 810
- height = 1440

Коэффициент масштаба:
scale = min(viewportWidth / 810, viewportHeight / 1440)

Требования:
- все ключевые размеры интерфейса должны зависеть от scale:
  - размеры шрифтов
  - размеры кнопок
  - размеры интерактивных объектов
  - отступы
  - радиусы
  - размеры HUD
- запрещено задавать основной UI только фиксированными px без общего scale
- запрещено ограничивать основной игровой контейнер жёстким max-width / max-height, если это делает игру визуально слишком мелкой на больших экранах
- игровые объекты должны масштабироваться тем же scale, что и UI
- базовые размеры визуальных игровых объектов должны быть заданы централизованно в конфигурационном блоке; итоговые размеры должны вычисляться через эти базовые значения и общий scale
- итоговый интерфейс должен сохранять читаемость и удобство на малых и больших вертикальных экранах
- текст внутри кнопок, карточек, HUD и модальных окон должен масштабироваться тем же scale, что и контейнер
- любой видимый текстовый UI-элемент, включая служебные подписи, ярлыки, хинты, подписи к контролам, drag-handle и аналогичные элементы, должен масштабироваться тем же scale, что и соответствующий контейнер
- общему scale должны подчиняться не только контейнеры и шрифты, но и связанные с ними отступы, радиусы, рамки, hit area, декоративные подложки, ярлыки, ручки перетаскивания и другие вспомогательные UI-элементы

---

### 📱 Расположение элементов управления

- элементы управления не должны располагаться только в верхней или нижней части экрана (зоны, куда сложно дотянуться на мобильных устройствах)

- рекомендуется размещать элементы управления:
  - в центральной части экрана
  - справа или слева от центра
  - либо по центру, если это не мешает игровому процессу

---

### 🎛 Группировка и перемещение

- элементы управления должны быть сгруппированы:
  - компактно
  - в одном блоке

- рекомендуется использовать перемещаемую панель управления:
  - панель должна иметь область (например, "ручку" или "ярлык") для перетаскивания
  - пользователь должен иметь возможность перемещать панель в удобное место на экране

---

### 🕹 Тип управления

- для управления направлением рекомендуется использовать круговой контрол (virtual joystick), а не отдельные кнопки

- круговой контрол:
  - представляет собой область (обычно круг), в которой пользователь задаёт направление движением пальца
  - обеспечивает плавное и естественное управление
  - должен поддерживать непрерывное направление (не только 4 стороны)

---

### ❌ Не рекомендуется

- использовать отдельные кнопки:
  - "вверх / вниз / влево / вправо"
- размещать кнопки управления разнесённо по экрану

---

### ✅ Допустимые исключения

- кнопки допустимы, если:
  - управление дискретное (например: выбор, прыжок, действие)
  - механика не требует направления движения

---

## 🧩 ШАБЛОН ДЛЯ AI

```html
<!doctype html>
<html>
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
</head>
<body>

<script>
let difficulty = 3;

window.addEventListener("message", (event) => {
  if (!event.data || event.data.type !== "gameInit") return;
  difficulty = event.data.difficulty ?? 3;
  startGame();
});

function startGame(){}

function finishGame(result){
  parent.postMessage({
    type: "gameResult",
    result: result
  }, "*");
}
</script>

</body>
</html>
```

---

# 🏁 ИТОГ

- сценарий управляет
- игра возвращает result
- ветвление через result

Этот файл можно напрямую использовать как промпт для генерации игр.
